# Copie para .env e ajuste os valores (o .env NÃO é versionado).
# O docker-compose injeta estas variáveis no container do order-service.

# Segredo do JWT (HS256, >= 32 bytes). Em produção use um valor forte e secreto.
APP_JWT_SECRET=0f75da60a576f8c7aedc5bb75b19d221abcd68d61df47a078a3b5117722dc2fd
